int		ft_main_what(t_exectoken *tmp)
{
	int		i;

	i = -1;
	while (tmp)
	{
		if (tmp->left != NULL)
		{
			;
			//pipe();
		}
		while (tmp->left != NULL)
			tmp = tmp->left;
		if (tmp->right != NULL)
			tmp = tmp->right;
		if (tmp->right != NULL)
			ft_whatis(tmp);
		if (tmp->right == NULL && tmp->left == NULL)
			break ;
	}
	return (1);
}
